import React from "react";

function ProjectDesc({
  name,
  position,
  duration,
  gitbub,
  description,
  stack,
  contribution,
}) {
  // console.log(name, "props-desc");
  return <div>{name}</div>;
}

export default ProjectDesc;
